'use server'

export default async function Post(
    title: string,
    content: string,
    bounty: number
): Promise<void> {
    // Get user address
    // Check user balance
    // Reduce the balance before posting
}